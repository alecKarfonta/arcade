services:
  app:
    build: .
    image: arcade-app:local
    ports:
      - "31021:31021"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:31021/health"]
      interval: 30s
      timeout: 10s
      retries: 3
