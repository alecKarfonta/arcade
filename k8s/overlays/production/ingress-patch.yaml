apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arcade
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - mlapi.us
      secretName: arcade-tls
